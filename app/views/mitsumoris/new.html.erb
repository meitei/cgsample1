
<div class="container">
	<div class="title">見積入力</div>
    <div class="statuslabel"><span>STEP 1</span>使用環境　座位保持装置製作方法の選定</div>
<%= render 'form' %>
<script>
  var step = 1;
  var mitsumoriData = {};
  var productsData = {};
  var tanka = {};
  var konyuRirekiId = 1;
  var kokyakuId = 1;
  var mitsumoriNo = 1;

  $(document).ready(function(){
    $.ajax({
        scriptCharset:"UTF-8",
        type: "GET",
        url: "/mitsumori_tankas.json",
        //data: postData,
        root: "mitsumori_tankas",
        dataType: "json",
        success: function(result) {
            if(result != null) {
                tanka = result;
            }
        }
    });
    $("#estimateForm").wizard();
    $("#btnEstimateRegist").hide();
    $("div#step1").load("/mitsumori/step_01.html", wizard1);
    // 見積データを用意する
    mitsumoriData["konyuRirekiId"] = konyuRirekiId;
    mitsumoriData["kokyakuId"] = kokyakuId;
    mitsumoriData["mitsumoriNo"] = mitsumoriNo;
    //clearNavi("1");
  });

  /*
   * 丸めモード、小数点以下の桁数を指定して、数値をフォーマットする。
   *  
   * kingaku 処理対象の金額
   * round 丸めモード（1：切り捨て、2：四捨五入、3：切り上げ）
   * digits 小数点以下の桁数（0：なし、1：１桁、2：２桁）
   */
  function getFormatAmount(kingaku, round, digits) {
    // 絶対値を取得
    var abs = Math.abs(kingaku);
    // 桁数処理用数値
    var num = Math.pow(10, digits);
    // 金額を数字に変換
    var amount = parseFloat(abs) * num;
    // 四捨五入・切り上げ・切り捨てを求める
    if(round == 1) {
      amount = Math.floor(amount) / num;
    } else if(round == 2) {
      amount = Math.round(amount) / num;
    } else if(round == 3) {
      amount = Math.ceil(amount) / num;
    } else {
      return kingaku;
    }
    if(kingaku < 0) {
      return amount * -1;
    } else {
      return amount;  
    }
  }

  /*
   * 桁区切りを追加し、金額をフォーマットする。
   *  
   * amount 処理対象の金額
   */
  function formatComma(amount) {
    var num = new String(amount).replace(/,/g,"");
    while(num != (num = num.replace(/^(-?\d+)(\d{3})/,"$1,$2")));
    return num;
  }

  /*
   * 整数に変換する。(null、undefined等は0)
   *  
   * val 処理対象
   */
  function parseFloatEx(val) {
    return val ? parseFloat(val) : 0;
  }

  function updateUnitPrice(step){
        // 単価を書き換え
    for(var i=0; i<tanka.length; i++) {
      $("div#" + step + " td#1-" + tanka[i]["seihinNo"] + "_tan").text(tanka[i]["tanka"]);
      $("div#" + step + " td#1-" + tanka[i]["seihinNo"] + "_kin").text(tanka[i]["tanka"]);
      $("div#" + step + " input#1-" + tanka[i]["seihinNo"] + "_tax").val(
        parseFloatEx(getFormatAmount(tanka[i]["tanka"]) * parseFloatEx(tanka[i]["tax"]), 1, 0));
      $("div#" + step + " td#2-" + tanka[i]["seihinNo"] + "_tan").text(tanka[i]["tanka"]);
      $("div#" + step + " td#2-" + tanka[i]["seihinNo"] + "_kin").text(tanka[i]["tanka"]);
      $("div#" + step + " input#2-" + tanka[i]["seihinNo"] + "_tax").val(
        parseFloatEx(getFormatAmount(tanka[i]["tanka"]) * parseFloatEx(tanka[i]["tax"]), 1, 0));
      $("div#" + step + " td#3-" + tanka[i]["seihinNo"] + "_tan").text(tanka[i]["tanka"]);
      $("div#" + step + " td#3-" + tanka[i]["seihinNo"] + "_kin").text(tanka[i]["tanka"]);
      $("div#" + step + " input#3-" + tanka[i]["seihinNo"] + "_tax").val(
        parseFloatEx(getFormatAmount(tanka[i]["tanka"]) * parseFloatEx(tanka[i]["tax"]), 1, 0));
      $("div#" + step + " td#4-" + tanka[i]["seihinNo"] + "_tan").text(tanka[i]["tanka"]);
      $("div#" + step + " td#4-" + tanka[i]["seihinNo"] + "_kin").text(tanka[i]["tanka"]);
      $("div#" + step + " input#4-" + tanka[i]["seihinNo"] + "_tax").val(
        parseFloatEx(getFormatAmount(tanka[i]["tanka"]) * parseFloatEx(tanka[i]["tax"]), 1, 0));
    }
  }

  function clearNavi(step) {
    if(step == "1") {
      $("div#step2").empty();
      $("li#step2 a").attr("href", "");
      $("li#step2 span").html("&nbsp;");
    }
    if(step == "2") {
      $("div#step3").empty();
      $("li#step3 a").attr("href", "");
      $("li#step3 span").html("&nbsp;");
    }
    $("div#step4").empty();
    $("li#step4 a").attr("href", "");
    $("li#step4 span").html("&nbsp;");
    $("div#step5").empty();
    $("li#step5 a").attr("href", "");
    $("li#step5 span").html("&nbsp;");
    $("div#step6").empty();
    $("li#step6 a").attr("href", "");
    $("li#step6 span").html("&nbsp;");
    $("div#step7").empty();
    $("li#step7 a").attr("href", "");
    $("li#step7 span").html("&nbsp;");
    $("div#step8").empty();
    $("li#step8 a").attr("href", "");
    $("li#step8 span").html("&nbsp;");
    $("div#step9").empty();
    $("li#step9 a").attr("href", "");
    $("li#step9 span").html("&nbsp;");
    $("div#step10").empty();
    $("li#step10 a").attr("href", "");
    $("li#step10 span").html("&nbsp;");
    $("div#step11").empty();
    $("li#step11 a").attr("href", "");
    $("li#step11 span").html("&nbsp;");
    $("div#step12").empty();
    $("li#step12 a").attr("href", "");
    $("li#step12 span").html("&nbsp;");
    $("div#step13").empty();
    $("li#step13 a").attr("href", "");
    $("li#step13 span").html("&nbsp;");
    $("div#step14").empty();
    $("li#step14 a").attr("href", "");
    $("li#step14 span").html("&nbsp;");
    $("div#step15").empty();
    $("li#step15 a").attr("href", "");
    $("li#step15 span").html("&nbsp;");
    $("div#step16").empty();
    $("li#step16 a").attr("href", "");
    $("li#step16 span").html("&nbsp;");
    $("div#step17").empty();
    $("li#step17 a").attr("href", "");
    $("li#step17 span").html("&nbsp;");
  }

</script>
<script src="/js/mitsumori/wizard17.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard16.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard15.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard14.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard13.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard12.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard11.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard10.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard09.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard08.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard07.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard06.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard05.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard04.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard03.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard02.js" type="text/javascript"></script>
<script src="/js/mitsumori/wizard01.js" type="text/javascript"></script>