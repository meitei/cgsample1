<% content_for :title do %>
見積完成図 - Assort 座位保持装置選定支援・補装具管理ソリューション
<% end %>
<div class="container" id="mainmenu_box" style="padding:20px 35px 0px;">
  <div class="title_master">見積完成図</div>

  <div class="mainmenu_inner cf">
    <ul>
      <% @array.each_with_index do |row, i| %>
       <li style="margin-bottom:5px;"><%= image_tag("/images/items/" + row , :id => "kanseizu_" + (i+1).to_s, :name => "kanseizu", :size => "200x200") %></li>
      <% end %>
    </ul>
  </div>
</div>
<script>
$(function() {
  $("ul#menu").hide();
  $("div#backtop").hide();
  $("div#info").hide();
});
</script>